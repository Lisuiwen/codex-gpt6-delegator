# Luna tier binding: exploration and search.
# Replace only the model ID if the installed catalog uses another compatible ID.
model = "gpt-5.6-luna"
model_provider = "openai"
model_reasoning_effort = "low"
